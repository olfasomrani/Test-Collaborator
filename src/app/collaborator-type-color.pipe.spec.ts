import { CollaboratorTypeColorPipe } from './collaborator-type-color.pipe';

describe('CollaboratorTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CollaboratorTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
